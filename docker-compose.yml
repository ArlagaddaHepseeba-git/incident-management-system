version: '3'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: incidents
    ports:
      - "5432:5432"

  redis:
    image: redis
    ports:
      - "6379:6379"
